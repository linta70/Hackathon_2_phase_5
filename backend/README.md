---
title: Hackhathon_2_Backend
emoji: "🧠"
colorFrom: "indigo"
colorTo: "blue"
sdk: docker
python_version: "3.10"
pinned: false
---

# Todo Backend API with AI Chatbot Integration

FastAPI backend for The Evolution of Todo with an AI assistant.

Run locally:

1. Install dependencies: `pip install -r requirements.txt`
2. Configure environment variables in the Space settings (Settings → Secrets); do NOT commit a `.env` file.
3. Run: `uvicorn app.main:app --reload`
